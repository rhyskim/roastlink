# RoastLink 웹사이트 — 남은 작업

## 디자인
- [x] **색상 팔레트 재검토** — 완료(2026-09-01). 크림+시에나(AI 흔한 배색) →
      **Roaster Flame(#FF6B35, 주요 버튼/액센트) / Warm Amber(#FFB703, 상태
      배지) / Deep Espresso(#121212, 배경) / Carbon Grey(#1E1E1E, 서피스) /
      Soft Chalk(#E0E0E0, 텍스트)** 다크 팔레트로 교체(사용자 최종 확정 값).
      `assets/css/style.css`의 `:root` 토큰 전면 교체 + 기존에 토큰을 안 쓰고
      하드코딩돼 있던 코드블록/배지/경고 콜아웃 색상도 이번에 같이 토큰화함.
- [x] 다크 모드 — 위 팔레트 자체가 다크 테마라 사실상 기본 적용됨(단, 라이트/다크
      **토글**은 아직 없음 — 항상 다크 고정. 라이트 모드도 원하면 별도 작업 필요).
- [ ] **홈페이지 UI 대대적 개편 예정**(2026-09-01) — 지금 레이아웃은 임시, 착수 시
      별도 계획 필요(전체 구조부터 다시 검토)

## 콘텐츠
- [x] ~~실제 스크린샷 촬영~~ → 아직 못 함, 이 항목만 유지: 실제 스크린샷 촬영(Artisan
      실 화면, 확정된 올바른 설정 상태 기준 — Quantifier 끔, Messages 필드 비움,
      Slider Step 5)
- [x] 가이드 실제 문구 작성(한/영/중) — 완료(2026-09-01). **1페이지 스크롤형 +
      우측 고정(Notion식) 목차로 재구성됨**(기존 7페이지 분리 구조 폐기)
- [x] FAQ 실제 내용 작성 — 완료(2026-09-01), 번호 매김(이모지 없음) + 항목별 박스
      분리로 가독성 개선까지 반영됨
- [ ] 중국어 콘텐츠 — Artisan이 중국어 UI를 지원하는지 미확인, 확인 필요(지원 안 하면
      영어 화면 그대로 두고 캡션만 번역). 지금 중국어 문구는 1차 초안(원어민 검수 전)
- [ ] 번역 검수 방법 확정(기계번역 초안 후 사용자 검수 vs 처음부터 검토하며 진행)
- [x] **"베타(Beta)" 배지 표시** — 홈/다운로드 페이지에 반영 완료
- [x] **피드백 위젯 — Formspree 엔드포인트 연결 완료**(2026-09-01) — 실제 폼
      (`https://formspree.io/f/xjyvyglv`)로 `_build/build.py`의
      `FORMSPREE_ENDPOINT`를 교체하고 테스트 제출로 `{"ok":true}` 응답까지 확인.
      무료 티어(월 50건) 한도만 참고.

## 배포/인프라
- [x] **v1.0.0-beta 릴리스 등록 + 다운로드 페이지 연동** — 완료(2026-09-01).
      exe + `.aset` 프리셋을 zip으로 묶어 `roastlink` 저장소 릴리스에 첨부,
      다운로드 버튼은 릴리스 목록 페이지가 아니라 파일로 바로 연결되는
      `/releases/latest/download/RoastLink-windows.zip` 사용(GitHub 자동
      "Source code" 링크를 사용자가 볼 일이 없도록). 버튼 옆에 GitHub API
      기반 누적 다운로드 수도 표시.
- [x] **호스팅 — GitHub Pages 대신 Cloudflare로 전환**(2026-09-01). 처음엔
      GitHub Pages(`rhyskim.github.io/roastlink/`)로 켰다가, `rhyskim.github.io`
      가 이미 개인 사이트로 쓰이고 있어서 롤백 — Cloudflare로 완전히 별도
      도메인 배포. 다만 "Connect to Git" 흐름이 예상한 classic Pages가 아니라
      **Workers + Static Assets** 방식으로 잡혀서 최종 주소가
      `https://roastlink.rhyskim.workers.dev`(`.pages.dev` 아님). 기능은
      동일(무료, 전 세계 CDN, git push 시 자동 재배포), 사용자가 이대로 두기로
      확정. `.assetsignore`로 `.git`/`.wrangler`/`_build` 등 저장소 내부 파일이
      실수로 공개 서빙되던 문제도 발견 즉시 수정함.
- [ ] 커스텀 도메인(추후, 상업 전환 시점에 고려 — 지금은 workers.dev 그대로 유지)

## 상업화 (2026-09-01 확정 — v2 착수 시점 작업, 지금은 코드 변경 없음)
- [ ] **현재 버전(v1)은 영구 무료 고정** — 나중에 이 버전을 유료로 강제 전환하지 않음.
      사이트에 "베타, 무료"로 명시(위 콘텐츠 항목 참고).
- [ ] **v2부터 유료 전환** — 버그 수정 + 새 기능 포함, 기존 v1 무료 사용자에게 소급 적용 안 함.
- [ ] v2 결제/라이선스는 자체 서버 없이 Gumroad 또는 LemonSqueezy의 라이선스 키 발급·검증
      API 사용(택 1, 아직 미결정). 앱에 라이선스 키 입력 화면 + 검증 로직 추가 필요
      (오프라인 로스터리 사용 고려 — 최초 1회 온라인 확인 후 로컬 캐시, 주기적 재확인 방식).
- [ ] 무료(v1 동결)와 유료(v2+) 기능 경계 정의
- 상세 배경: 소스 저장소 `plan.md` §3-7 참고.
